{
  "name": "mu-level-watcher-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:apply:local": "wrangler d1 migrations apply mu-watcher --local",
    "db:apply:remote": "wrangler d1 migrations apply mu-watcher --remote",
    "typecheck": "tsc --noEmit",
    "check:ui": "node scripts/check-ui.mjs",
    "check": "npm run typecheck && npm run check:ui"
  },
  "dependencies": {
    "@cf-wasm/photon": "^0.3.4",
    "@cf-wasm/resvg": "^0.3.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
